{
  "build": {
    "builder": "nixpacks",
    "config": {
      "startCommand": "python manage.py collectstatic --noinput && python manage.py migrate && gunicorn buddyworthcalc.wsgi",
      "installCommand": "pip install -r requirements.txt"
    }
  }
}